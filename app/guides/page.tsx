import type { Metadata } from "next";import Link from "next/link";import { ArrowRight } from "lucide-react";import { GUIDES } from "@/lib/guides";
export const metadata:Metadata={title:"Irish Home-Buying Guides",description:"Clear, sourced guides to stamp duty, surveys, snagging, insurance, and Help to Buy in Ireland."};
export default function GuidesPage(){return <main><section className="page-hero"><div className="container"><span className="eyebrow"><span className="dot"/>Buyer guides</span><h1>Short answers for the tabs still open in your head.</h1><p className="lead">Plain-English orientation, linked back to official sources.</p></div></section><section className="section"><div className="container guide-grid">{Object.entries(GUIDES).map(([slug,guide])=><Link className="card" href={`/guides/${slug}`} key={slug}><h3>{guide.title}</h3><p>{guide.description}</p><span className="card-link">Read guide <ArrowRight size={14}/></span></Link>)}</div></section></main>}
