import { notFound } from "next/navigation";import type { Metadata } from "next";import { GUIDES } from "@/lib/guides";import type { GuideSlug } from "@/lib/guides";
export function generateStaticParams(){return Object.keys(GUIDES).map(slug=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const guide=GUIDES[slug as GuideSlug];return guide?{title:guide.title,description:guide.description}:{} }
export default async function GuidePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const guide=GUIDES[slug as GuideSlug];if(!guide)notFound();const faq={"@context":"https://schema.org","@type":"Article",headline:guide.title,description:guide.description,dateModified:"2026-07-03"};return <main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(faq)}}/><section className="page-hero"><div className="container"><span className="eyebrow"><span className="dot"/>Irish buyer guide</span><h1>{guide.title}</h1><p className="lead">{guide.description}</p></div></section><article className="guide-body">{guide.sections.map(section=><section key={section.heading}><h2>{section.heading}</h2><p>{section.body}</p></section>)}<div className="notice" style={{marginTop:40}}>This is general information, not legal, tax, insurance, mortgage, or financial advice. Confirm how the rules apply to you with the relevant regulated professional or authority.</div><p style={{marginTop:25}}>Official source: <a className="source" href={guide.source} target="_blank" rel="noreferrer">{guide.sourceLabel}</a></p><p className="disclaimer">Last checked: 3 July 2026.</p></article></main>}
