# Optional. Without this, document analysis uses a clearly-labelled local demo analyser.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Comma-separated operations inboxes for lead notifications (adapter not enabled in demo).
LEAD_NOTIFICATION_EMAIL=
